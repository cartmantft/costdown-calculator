# Local dev server (never exposed to the client)
DEV_SERVER_HOST=0.0.0.0
DEV_SERVER_PORT=5173

# Client runtime configuration
VITE_DCA_HISTORY_LIMIT=10
VITE_APP_CURRENCY_SYMBOL=원
VITE_APP_CURRENCY=KRW
